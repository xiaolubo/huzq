/** 
 * 系统主页的底部区域，主要放置用户单位信息，服务单位和服务人员信息 
 */  
Ext.define('sdkinte.view.main.region.Bottom', {  
  
            extend : 'Ext.toolbar.Toolbar',  

            alias : 'widget.mainbottom',  
  
            items : [{  
                        bind : {  
                            text : '使用单位:{user.company}'  
                        }  
                    }, {  
                        bind : {  
                            text : '用户:{user.name}'  
                        }  
                    },'->', {  
                        bind : {  
                            text : '©{service.copyright}'  
                        }  
                    }]  
        });  